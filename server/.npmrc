# Allow Puppeteer to download Chrome on this server.
# The root .npmrc skips download for the frontend (Vercel).
# This file overrides that for the PDF server (Render).
PUPPETEER_SKIP_DOWNLOAD=false
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=false
