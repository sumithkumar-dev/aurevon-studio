{
  "name": "aurevon-pdf-server",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node pdf-server.js",
    "build": "npx puppeteer browsers install chrome || echo 'Puppeteer Chrome install failed, will use system Chrome'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^22.0.0"
  }
}
