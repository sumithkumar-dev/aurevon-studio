# Prevent Puppeteer from downloading Chromium during `npm install`.
# The PDF server (server/) manages its own Chromium via its own package.json.
# Vercel only installs the root package.json -- this ensures the build never
# tries to download a 170MB Chromium binary it doesn't need.
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
